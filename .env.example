# Optional. When set, /api/notes/generate calls Anthropic for real model output.
# When absent, generation falls back to a deterministic offline note crafted
# against the seeded transcript so the demo runs end-to-end without a key.
ANTHROPIC_API_KEY=

# Optional. Defaults to claude-sonnet-4-6.
# ANTHROPIC_MODEL=
