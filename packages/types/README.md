# @clinical-notes/types

Shared TypeScript types and Zod schemas. The contract layer between every TS app and package — and between TS and the Python services via JSON Schema export.

Owned by `architect` (schema shape) and `backend-engineer` (implementation).
