# @clinical-notes/auth

RBAC primitives. The session shape (`AuthContext`) and the `can` / `require` permission checks. Session creation, MFA, and SSO wiring live in `apps/api`. Owned by `backend-engineer`.
